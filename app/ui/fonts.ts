import { Inter, Lusitana } from 'next/font/google';
import {undefined} from "zod";

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({adjustFontFallback: false, weight: "700", subsets: ['latin']});